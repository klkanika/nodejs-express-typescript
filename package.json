{
  "name": "nodejs-express-typescript",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap:ci": "lerna bootstrap --ci",
    "bootstrap": "lerna bootstrap",
    "bootstrap:clean": "lerna run clean && lerna bootstrap",
    "build": "lerna run build",
    "pub": "lerna publish",
    "test": "lerna run test"
  },
  "dependencies": {
    "@types/node": "^14.14.37"
  },
  "devDependencies": {
    "doctoc": "~2.0.0",
    "eslint": "~7.21.0",
    "lerna": "~3.22.0",
    "typescript": "~4.2.0"
  }
}
